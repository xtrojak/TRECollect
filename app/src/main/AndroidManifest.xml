<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for OpenStreetMap -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    
    <!-- Permissions for Camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TRECCustomLogsheets"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.SiteDetailActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ui.SettingsActivity"
            android:parentActivityName=".MainActivity"
            android:label="Settings" />
        <activity
            android:name=".ui.FormEditActivity"
            android:parentActivityName=".ui.SiteDetailActivity"
            android:label="Edit Form" />
           <activity
               android:name=".ui.GPSPickerActivity"
               android:parentActivityName=".ui.FormEditActivity"
               android:label="Select GPS Location" />
           <activity
               android:name=".ui.OfflineMapsActivity"
               android:parentActivityName=".ui.SettingsActivity"
               android:label="Offline Maps" />
           <activity
               android:name=".ui.DownloadRegionActivity"
               android:parentActivityName=".ui.OfflineMapsActivity"
               android:label="Download Map Region" />
           <activity
               android:name=".ui.MapPreviewActivity"
               android:parentActivityName=".ui.OfflineMapsActivity"
               android:label="Map Preview" />
        
        <!-- FileProvider for photo capture -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>

